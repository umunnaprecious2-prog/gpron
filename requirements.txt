fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0
# Needed specifically for CockroachDB Serverless (see DATABASE_URL scheme
# note in .env.example): SQLAlchemy's plain postgresql+asyncpg dialect
# fails against CockroachDB on two dialect-internals mismatches (a "json"
# type it doesn't have, and a non-Postgres server-version string). This
# dialect patches around both. Not needed for Neon/Supabase/RDS/local
# Postgres, which all use the plain postgresql+asyncpg scheme.
sqlalchemy-cockroachdb>=2.0.4
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 (unmaintained since ~2020) breaks against bcrypt>=4.1.0,
# which removed the __about__ attribute passlib's version-detection reads
# at import time (AttributeError: module 'bcrypt' has no attribute
# '__about__', surfacing as password hashing failing outright). Pinned
# below that. Was masked locally by an older bcrypt already installed from
# before this became an issue -- a clean install caught the gap.
bcrypt<4.1.0
python-dotenv==1.0.1
pydantic>=2.7.4
pydantic-settings>=2.6.0
python-multipart==0.0.9
email-validator==2.1.1
httpx>=0.27.0
google-auth>=2.27.0
# google.auth.transport.requests (used in app/services/google_auth_service.py)
# needs this at import time; google-auth doesn't pull it in as a hard
# dependency. Was masked locally by an already-installed transitive copy
# from another package -- Render's clean environment caught the gap.
requests>=2.31.0
ruff==0.4.4
black==24.4.2
pytest==8.2.0
pytest-asyncio==0.23.6
